#include <stdio.h>
#include <sys/socket.h>
void main()
{
	while(1){
		printf("zaki\n");
		int sfd;
		sfd = socket(AF_INET,SOCK_STREAM,NULL);
		printf("socketid = %d\n",sfd);
		
		bind 
		accept

		recevZZ
		while(1);

	}

}
